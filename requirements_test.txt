pytest
pytest-asyncio
pytest-homeassistant-custom-component

# The Bluetooth logic lives in this library (manifest requirement).
mvave-tankg-ble>=0.1.0

# Runtime deps of the HA bluetooth/usb components (installed by HA at runtime,
# needed explicitly for the test harness).
aiousbwatcher
habluetooth
bluetooth-adapters
bluetooth-data-tools
bleak
bleak-retry-connector
dbus-fast
